import { css } from "lit";

export const homeStyles = css`
    .home-main {
        max-width: 800px;
        margin-left: auto;
        margin-right: auto;
    }

    .search-settings-container {
        margin: 20px;
        gap: 20px;
    }

    .text-preview {
        overflow: auto;
        background-color: var(--sl-color-neutral-100);
    }
`;